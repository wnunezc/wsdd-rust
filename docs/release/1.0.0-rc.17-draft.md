# Release Draft — WSDD 1.0.0-rc.17

## Status

Draft release notes prepared locally for `WP-09` validation. Do not create a public GitHub Release unless explicitly approved.

## Recommended package

- **Primary asset:** `wsdd-1.0.0.17-x86_64.msi`
- **SHA256:** `782135944A02FF27510B07EA3EDF21B4EBD0639A6F81AB7C60C99901BDF1CA08`
- **Size:** `21,262,336` bytes
- **Target audience:** Windows 10 / 11 users running WSDD with administrator privileges
- **Distribution channel:** Local/private validation until publication is explicitly approved

## Highlights

- `WP-09` option `B` is in review: unit tests, isolated integration checks, elevated manual checklist, and release smoke validation.
- Added `docs/release-validation.md` as the release validation strategy.
- Added isolated integration checks for package version alignment, validation strategy coverage, and release MSI/checksum workflow evidence.
- Prepared `RC17` locally for validation only.

## Validation

- `cargo fmt --all`
- `cargo check --workspace --target-dir .\target\wp09-check`
- `cargo clippy --workspace --target-dir .\target\wp09-clippy -- -D warnings`
- `WSDD_SKIP_WINDOWS_MANIFEST=1 cargo test --workspace --target-dir .\target\wp09-test`
  - `wsdd`: 64 passed.
  - `tests/release_validation.rs`: 3 passed.
  - `wsdd-launcher`: 3 passed.
  - `xtask`: 0 tests.
- `cargo wix-msi --nocapture`
- `wsdd.exe` and `wsdd-launcher.exe` file/product versions verified as `1.0.0-rc.17`.

## Scope notes

- this build does not close `WP-09`
- no updater work
- no public GitHub Release
- no commit created
- elevated manual validation remains pending

## Suggested release summary

WSDD `1.0.0-rc.17` packages the first `WP-09` validation strategy lot for local MSI testing. It adds a documented release validation matrix, isolated integration checks for release metadata, and a local `RC17` smoke artifact.
