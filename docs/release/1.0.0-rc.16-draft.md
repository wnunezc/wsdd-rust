# Release Draft — WSDD 1.0.0-rc.16

## Status

Draft release notes prepared locally for manual MSI validation. Do not create a public GitHub Release unless explicitly approved.

## Recommended package

- **Primary asset:** `wsdd-1.0.0.16-x86_64.msi`
- **SHA256:** `C99F611616F8F6E03C4057457108CC757D14440EE219A081D8BD79A4B572FAC1`
- **Target audience:** Windows 10 / 11 users running WSDD with administrator privileges
- **Distribution channel:** Local/private validation until publication is explicitly approved

## Highlights

- `RC16` continues `WP-07` validation with SRP refactors applied across backup, Docker, deploy, settings, app/UI, project model, loader, hosts, and PowerShell script handling.
- Public facades remain stable for the refactored modules.
- `WP-07` was closed by explicit user validation after the local `RC16` MSI was generated.
- `src/ui/helps.rs` remains deferred to `WP-08`.

## Validation

- `cargo fmt --all`
- `cargo check --workspace`
- `cargo clippy --workspace -- -D warnings`
- `WSDD_SKIP_WINDOWS_MANIFEST=1 cargo test --workspace --target-dir .\target\rc16-local-test`
- `cargo wix-msi --nocapture`

## Scope notes

- this build closes `WP-07`
- no updater work
- no backlog visual work
- no public GitHub Release
- no commit created

## Suggested release summary

WSDD `1.0.0-rc.16` packages the current `WP-07` SRP refactor state for local MSI testing. This build keeps public APIs stable while splitting large mixed-responsibility modules into focused internal submodules and preserving the existing installer flow.
